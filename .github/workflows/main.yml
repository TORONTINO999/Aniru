name: Обновление плейлиста аниме-фильмов

on:
  schedule:
    - cron: '0 6 * * 1'   # каждую неделю в 6:00 UTC (в понедельник)
  workflow_dispatch:      # возможность запуска вручную

jobs:
  update-playlist:
    runs-on: ubuntu-latest

    steps:
      - name: Клонирование репозитория
        uses: actions/checkout@v4

      - name: Установка Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Установка зависимостей Python
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Запуск парсера
        run: python parser.py

      - name: Коммит и пуш изменений
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add anime_movies.m3u posters_movies/ progress_movies.json
          git diff --staged --quiet || git commit -m "Автоматическое обновление плейлиста фильмов $(date -u +'%Y-%m-%d %H:%M:%S')"
          git push
